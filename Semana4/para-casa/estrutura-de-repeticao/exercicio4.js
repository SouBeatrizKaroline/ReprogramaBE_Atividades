// 4. Crie um array com 5 número.Crie um `while` que imprima cada número do array no console.

var numeros = [1, 2, 3, 4, 5];
var i = 0;

while (i < numeros.length) {
  console.log(numeros[i]);
  i++;
}
